# App runtime settings (used by app/settings.py)
DB_NAME=fastapi_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=0.0.0.0
DB_PORT=5432
DB_BATCH_SIZE=500
ATS_SCRAPER_DELAY_SECONDS=0.2

# Docker-only settings (used by docker-compose.yml)
POSTGRES_VERSION=13.4-alpine
